# MCP Atlassian Universal Proxy Configuration

# Server configuration
HOST=0.0.0.0
PORT=8001
LOG_LEVEL=info

# Optional: External port for docker-compose
MCP_PROXY_PORT=8001

# Optional: Custom NPM registry if using private registry
# NPM_REGISTRY=https://registry.npmjs.org/